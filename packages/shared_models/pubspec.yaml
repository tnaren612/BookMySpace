name: shared_models
description: Cross-app Dart models and enums (no Flutter UI)
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
